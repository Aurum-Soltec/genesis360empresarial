export default function Loading() {
  return (
    <main className="focused-shell">
      <section className="focused-panel" role="status" aria-live="polite">
        <p className="eyebrow">Genesis 360 Empresarial</p>
        <h1>Carregando conteúdo…</h1>
        <p>Estamos preparando a leitura da empresa selecionada.</p>
      </section>
    </main>
  );
}
